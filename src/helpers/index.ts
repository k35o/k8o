export type Status = 'success' | 'info' | 'warning' | 'error';

export type Direction = 'up' | 'down' | 'right' | 'left';

export { extractColor } from './color/extract-color';
export { findAllColors } from './color/find-all-colors';
