{
  "name": "@repo/database",
  "version": "1.0.0",
  "description": "Database client for k8o project",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/db.ts",
      "storybook": "./src/__mocks__/db.ts",
      "node": "./src/db.ts",
      "default": "./src/db.ts"
    },
    "./auth": {
      "types": "./src/auth.ts",
      "node": "./src/auth.ts",
      "default": "./src/auth.ts"
    }
  },
  "scripts": {
    "dev": "portless database sh -c 'turso dev --db-file local.db --port ${PORT:-8787}'",
    "type-check": "tsgo --noEmit",
    "generate": "drizzle-kit generate",
    "generate:custom": "drizzle-kit generate --custom",
    "migrate": "drizzle-kit migrate",
    "studio": "drizzle-kit studio",
    "export:schema": "drizzle-kit export > ./schema.sql",
    "build:erd": "liam erd build --input './src/schema/*.ts' --format drizzle",
    "preview:erd": "pnpm run build:erd && pnpm dlx http-server dist"
  },
  "dependencies": {
    "@libsql/client": "0.17.2",
    "@next/env": "16.2.4",
    "better-auth": "1.6.6",
    "drizzle-orm": "0.45.2"
  },
  "devDependencies": {
    "@liam-hq/cli": "0.7.24",
    "@repo/typescript-config": "workspace:*",
    "drizzle-kit": "0.31.10"
  }
}
