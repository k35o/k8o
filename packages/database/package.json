{
  "name": "@repo/database",
  "version": "1.0.0",
  "private": true,
  "description": "Database client for k8o project",
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/db.ts",
      "storybook": "./src/__mocks__/db.ts",
      "node": "./src/db.ts",
      "default": "./src/db.ts"
    },
    "./auth": {
      "types": "./src/auth.ts",
      "node": "./src/auth.ts",
      "default": "./src/auth.ts"
    },
    "./schema": {
      "types": "./src/schema/public.ts",
      "default": "./src/schema/public.ts"
    }
  },
  "scripts": {
    "dev": "node scripts/local-db.ts serve",
    "db:serve": "node scripts/local-db.ts serve",
    "db:status": "node scripts/local-db.ts status",
    "type-check": "tsgo --noEmit",
    "generate": "drizzle-kit generate",
    "generate:custom": "drizzle-kit generate --custom",
    "migrate": "drizzle-kit migrate",
    "studio": "drizzle-kit studio",
    "export:schema": "drizzle-kit export > ./schema.sql",
    "build:erd": "liam erd build --input './src/schema/*.ts' --format drizzle",
    "preview:erd": "pnpm run build:erd && pnpm dlx http-server dist"
  },
  "dependencies": {
    "@libsql/client": "0.17.3",
    "better-auth": "1.6.14",
    "drizzle-orm": "catalog:"
  },
  "devDependencies": {
    "@liam-hq/cli": "0.7.24",
    "@next/env": "16.2.7",
    "@repo/typescript-config": "workspace:*",
    "drizzle-kit": "0.31.10"
  }
}
