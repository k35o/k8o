import { fn } from 'storybook/test';

export const getBlogContents = fn().mockName('getBlogContents');

export const getBlogContent = fn().mockName('getBlogContent');

export const getBlogToc = fn().mockName('getBlogToc');

export const getBlogsByTags = fn().mockName('getBlogsByTags');

export const getBlogReadingTime = fn().mockName('getBlogReadingTime');
