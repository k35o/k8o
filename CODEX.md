# CODEX.md

Codex CLI 用の補足ルールです。共通事項は `AGENTS.md` に従ってください。

## 基本

- ファイル探索は `rg` を優先（高速検索）
- 単一ファイルの小規模編集は `apply_patch` を優先
- レビュー依頼時は、リスクと回帰を優先して指摘する

## 変更時の方針

- 既存の設計・命名・構造を尊重する
- 目的が不明確な場合は質問してから作業する
